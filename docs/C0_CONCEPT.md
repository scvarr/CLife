# C0 — концептуальная модель CLife

Статус: **draft / обсуждается**

Этот документ фиксирует концептуальные решения проекта до перехода к программной архитектуре и реализации.

На C0 сначала определяется **что существует в моделируемом мире и как оно себя ведёт**, и только после этого — какими программными сущностями это будет представлено.

---

## 1. Граница детализации

### Принято

**Клетка — атомарная активная сущность симуляции.**

CLife не моделирует внутреннее устройство клетки на уровне:

- органелл;
- молекул;
- атомов;
- квантов;
- физических переносчиков внутри клетки.

Внутренние процессы клетки представлены агрегированными числовыми величинами и преобразованиями.

Для внешнего мира клетка концептуально выглядит так:

```text
Inputs + Parameters + Current State
                |
                v
          Cell Functions
                |
                v
        New State + Outputs
```

Клетка получает числа, преобразует числа и возвращает числа.

Это сознательная и фундаментальная граница масштаба модели.

---

## 2. Мир строится от минимально необходимого

### Принято

Проект не пытается заранее построить полную физическую вселенную.

Новая фундаментальная сущность, величина или закон вводятся только тогда, когда без них невозможно выразить требуемое поведение.

На стартовом уровне допустим мир, содержащий только:

- пространство/пустоту;
- первичную клетку;
- минимальный набор типов материи, необходимый текущей модели.

Первая клетка является **магическим начальным условием**. Проект не обязан объяснять её происхождение.

---

## 3. Материя

### Принято

`Organic` и `Inorganic` — **разные типы материи**, а не состояния одного вещества.

Минимальный набор:

```text
Organic[0]
Inorganic[0]
```

Индекс `[0]` означает первую определённую разновидность соответствующего семейства материи.

Новые разновидности не создаются заранее. Они добавляются только тогда, когда существующих свойств и типов недостаточно для требуемого поведения.

### 3.1. Количество материи

Количество материи — число.

```text
Cell:
    Organic[0] = 10
```

означает, что клетка содержит 10 условных единиц `Organic[0]`.

Это не означает существование десяти отдельных частиц внутри клетки.

### 3.2. Базовая шкала объёма

Стартовая относительная шкала:

```text
1 unit Organic[0] = 1 условная единица объёма
```

Это не метрическая система и не попытка привязаться к реальным нанометрам.

`Organic[0]` задаёт исходную шкалу, относительно которой позднее могут определяться другие типы материи.

---

## 4. Объём и начальная форма

### Принято

Количество структурной материи определяет объём объекта.

Для первой клетки, состоящей только из `Organic[0]`:

```text
volume = Organic[0] amount
```

На первом этапе форма представления клетки — сфера, объём которой соответствует рассчитанному объёму клетки.

```text
structural matter
      |
      v
    volume
      |
      v
geometric size
      |
      v
 visual sphere
```

Размер не задаётся отдельной произвольной цифрой, если его можно вывести из количества материи.

---

## 5. Взрослая клетка

### Принято — фундаментальное ограничение

**Взрослая клетка не растёт.**

Геном при формировании клетки сразу определяет:

- структурный состав;
- набор функций;
- параметры функций;
- исходные параметры phenotype;
- в будущем — морфологию.

После формирования взрослой клетки её набор функций фиксирован.

Обычный simulation tick:

- не добавляет новые функции;
- не удаляет функции;
- не увеличивает структурный размер клетки;
- не перестраивает phenotype.

Изменение phenotype возможно через изменение генома и формирование новой клетки.

### 5.1. Статическая и динамическая части

У взрослой клетки различаются:

```text
STATIC PHENOTYPE
    structural composition
    functions
    function parameters
    morphology

DYNAMIC STATE
    stored energy
    temperature
    stored materials
    other mutable values
```

Накопление энергии или материала не означает автоматического роста взрослой клетки.

---

## 6. Зачаток / зародыш

### Принято на концептуальном уровне

Рост существует только у специальной стадии клетки — зачатка/зародыша (`Embryo`, рабочее название).

Зародыш:

- не является уменьшенной взрослой клеткой;
- не выполняет обычный набор функций взрослого phenotype;
- получает строительный материал от родительской клетки;
- растёт только за счёт этой внешней накачки материалом;
- после выполнения условий формируется во взрослую клетку.

```text
Parent Adult Cell
       |
       | material transfer
       v
     Embryo
       |
       | structural material accumulation
       | + activation condition
       v
Genome interpretation / assembly
       |
       v
   Adult Cell
```

### 6.1. Activator

Предварительно предполагается некоторый `Activator`, определяющий, **во что именно должен сформироваться зародыш**.

Пока это только концептуальная граница, без детальной системы.

Уже принято:

- activator относится к этапу формирования клетки;
- activator не является механизмом постоянного перепрограммирования взрослой клетки;
- после формирования phenotype фиксирован.

---

## 7. Геном

### Принято

Геном — **чертёж законченной взрослой клетки**, а не программа её постепенного роста.

```text
Genome
   |
   v
formation / interpretation
   |
   v
Adult phenotype
```

После формирования клетки genome не должен интерпретироваться каждый simulation tick.

Набор функций взрослой клетки меняется только через изменение genome и последующее формирование новой клетки.

Формат genome, правила expression и связь с activator пока не определены.

---

## 8. Числовая модель клетки

### Принято

На концептуальном уровне различаются четыре роли числовых величин.

### Inputs

Числовые воздействия внешнего мира, доступные клетке в текущий момент.

Примеры будущих inputs:

```text
light
external temperature
radiation
chemical concentration
...
```

### Parameters

Числа, описывающие сформированный phenotype.

Они выводятся из genome, состава и функций клетки и не меняются обычным tick.

### State

Изменяемые числа конкретного экземпляра клетки.

Например:

```text
stored_energy
temperature
stored_material
...
```

### Outputs

Числовые воздействия клетки на внешний мир.

Конкретный набор outputs пока не определён.

---

## 9. Клеточные функции

### Принято

Функция взрослой клетки — агрегированное правило преобразования чисел.

Она может читать:

- inputs мира;
- параметры phenotype;
- текущее динамическое состояние.

И может изменять:

- динамическое состояние;
- outputs клетки.

Пример:

```text
light input
    |
    v
light conversion function
    |
    +--> stored_energy increases
    |
    +--> temperature increases
```

Нам не требуется моделировать физический механизм ниже этого уровня.

---

## 10. Свет и энергия

### Принято

Мы не используем упрощённую картину:

```text
world emits abstract energy
        ->
cell receives abstract energy
```

Свет — конкретный внешний переносчик/воздействие, которое может приносить клетке доступную энергию.

На раннем этапе мир может задавать простое однородное значение:

```text
light = 0.1
```

Для этого не нужны:

- renderer;
- объект источника света;
- полноценная сцена;
- пространственная трассировка света.

Клетка получает числовой input `light`.

Её функция преобразования определяет, как этот input меняет внутренние числа клетки.

Например:

```text
light
  |
  +--> stored_energy
  |
  +--> temperature
```

Свет может непосредственно нагревать клетку. Процесс преобразования света во внутренний энергетический запас также может давать тепловые потери.

### 10.1. Внутренняя энергия

На текущем масштабе **не моделируется конкретный химический или физический носитель внутренней энергии**.

`stored_energy` — агрегированное числовое состояние клетки.

Не вводятся ATP-подобные молекулы, фотоны внутри клетки и другие сущности ниже атомарного уровня клетки.

### 10.2. Что пока не решено

Понятие `Energy` как отдельная фундаментальная сущность мира пока не утверждено.

Нужно определить позднее:

- является ли энергия только общей мерой преобразований;
- какие внешние переносчики энергии реально нужны;
- как представить тепло;
- какие законы сохранения нужны нашей степени абстракции.

---

## 11. Органика и неорганика

### Принято направление

Клетка в будущем может иметь функцию преобразования `Inorganic` в `Organic`.

Такое преобразование не происходит автоматически только потому, что вещества находятся рядом.

После смерти органическая структура клетки может некоторое время оставаться органической материей, а затем под действием законов мира или внешних воздействий разлагаться.

Предварительная схема:

```text
Inorganic
    |
    | biological transformation
    v
Organic
    |
    | cell death
    v
Dead organic structure
    |
    | decay / world laws
    v
Inorganic + released effects
```

Смерть и разложение — разные процессы.

Конкретная энергетика разложения пока не определена.

---

## 12. Граница визуализации

### Принято

Симуляция выдаёт данные. Renderer строит по ним визуальное представление.

Например в будущем frontend может получить:

```text
composition
volume
temperature
stored values
morphology
attachment points
...
```

Godot, Unreal Engine или другой renderer не определяют семантику симуляции.

На первом этапе клетка может отображаться простой сферой, размер которой выводится из объёма.

---

## 13. Что сознательно не моделируется сейчас

Без отдельной необходимости не вводятся:

- атомы;
- молекулы;
- квантовая физика;
- органеллы;
- полноценная химия;
- периодическая система;
- SI/метрическая система;
- гравитация;
- масса;
- плотность как самостоятельная физика;
- детальная механика среды;
- полноценная пространственная модель света;
- рост взрослых клеток.

Если в будущем конкретный закон потребует одну из этих величин, она рассматривается отдельно в момент возникновения такой необходимости.

---

## 14. Текущий минимальный мир

```text
WORLD

Space:
    empty

Matter definitions:
    Organic[0]
    Inorganic[0]

Initial object:
    Primary Cell
        structural Organic[0] = N

Derived:
    volume = structural Organic[0]

Presentation:
    sphere with derived volume
```

`Inorganic[0]` может быть определён как возможный тип материи, даже если в самом первом тестовом мире его количество равно нулю.

Следующее динамическое расширение может добавить:

```text
World input:
    light = L

Adult cell function:
    light -> stored_energy + temperature change
```

---

## 15. Открытые вопросы C0

Ещё не утверждены:

1. Что именно представляет собой свет на уровне мира до появления полноценной пространственной среды.
2. Что такое энергия на уровне общей онтологии CLife.
3. Как представлять тепло и температуру.
4. Какие первые физические свойства материи реально необходимы.
5. Как универсально описывать клеточные функции-преобразования.
6. Как различать структурную материю и хранимый клеткой материал.
7. Как именно genome + activator формируют взрослый phenotype.
8. Как задаются условия готовности зародыша к формированию.
9. Какие первые outputs клетки нужны окружающему миру.

---

## 16. Historical reference

Старый Godot-проект используется только как:

- доказательство работоспособности отдельных идей;
- источник проверенных формул;
- golden reference для будущих интеграционных проверок.

```text
repository: https://github.com/scvarr/lab3d.git
accepted stage: R7
commit: 71a95bbc12b72102460e21045840d6dbd2f734ac
```

CLife не является механическим портом старого проекта.

---

## 17. Что этот документ пока не описывает

Этот файл не является архитектурным реестром реализации.

Здесь намеренно нет:

- C++ классов;
- файлов проекта;
- API;
- CMake;
- runtime ID;
- `PROJECT_MAP.yaml`;
- окончательного набора `Resource / Field / Property / ...`.

`PROJECT_MAP.yaml` должен появиться после того, как C0 будет согласован и фундаментальные понятия перестанут постоянно меняться.
